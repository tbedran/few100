

describe('writing specs in typescript', () => {
    it('is easy', () => {
        expect(true).toBe(true);
    });
    it('i can do things', () => {
        const a = 2, b = 3;
        expect(a + b).toBe(5);
    });
});
